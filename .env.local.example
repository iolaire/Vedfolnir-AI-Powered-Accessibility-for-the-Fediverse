# Local Environment Variables for Vedfolnir
# 
# This file contains sensitive environment variables that should NEVER be committed to version control.
# Copy this file to .env.local and fill in your actual values.
# 
# IMPORTANT: .env.local is already in .gitignore and will not be committed.
# 
# See docs/security/environment-setup.md for complete setup instructions.

# Flask Secret Key - Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
export FLASK_SECRET_KEY="your-generated-flask-secret-key-here"

# Admin User Configuration
export AUTH_ADMIN_USERNAME="admin"
export AUTH_ADMIN_EMAIL="admin@yourdomain.com"
export AUTH_ADMIN_PASSWORD="your-secure-admin-password-here"

# Platform Encryption Key - Generate with: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
export PLATFORM_ENCRYPTION_KEY="your-generated-encryption-key-here"

# Usage Instructions:
# 1. Copy this file: cp .env.local.example .env.local
# 2. Generate secure values using the commands above
# 3. Replace the placeholder values with your generated values
# 4. Source the file before running the application: source .env.local
# 5. Start the application: python web_app.py